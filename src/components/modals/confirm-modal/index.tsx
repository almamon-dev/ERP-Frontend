import React from 'react';

export interface ConfirmModalProps {
    className?: string;
}

export default function ConfirmModal({ className }: ConfirmModalProps) {
    return (
        <div className={className}>
            {/* ConfirmModal Component */}
        </div>
    );
}
