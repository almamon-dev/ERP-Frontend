import React from 'react';

export interface AccordionProps {
    className?: string;
}

export default function Accordion({ className }: AccordionProps) {
    return (
        <div className={className}>
            {/* Accordion Component */}
        </div>
    );
}
