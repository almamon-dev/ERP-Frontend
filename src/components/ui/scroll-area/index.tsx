import React from 'react';

export interface ScrollAreaProps {
    className?: string;
}

export default function ScrollArea({ className }: ScrollAreaProps) {
    return (
        <div className={className}>
            {/* ScrollArea Component */}
        </div>
    );
}
