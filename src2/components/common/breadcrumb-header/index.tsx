import React from 'react';

export interface BreadcrumbHeaderProps {
    className?: string;
}

export default function BreadcrumbHeader({ className }: BreadcrumbHeaderProps) {
    return (
        <div className={className}>
            {/* BreadcrumbHeader Component */}
        </div>
    );
}
