import React from 'react';

export interface DeleteModalProps {
    className?: string;
}

export default function DeleteModal({ className }: DeleteModalProps) {
    return (
        <div className={className}>
            {/* DeleteModal Component */}
        </div>
    );
}
