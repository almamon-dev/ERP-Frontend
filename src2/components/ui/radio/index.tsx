import React from 'react';

export interface RadioProps {
    className?: string;
}

export default function Radio({ className }: RadioProps) {
    return (
        <div className={className}>
            {/* Radio Component */}
        </div>
    );
}
